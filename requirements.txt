# requirements.txt
# WHY: The original notebooks imported `tensorflow`/`keras` but never used them
# (dead imports), pip-installed sentence_transformers inline, and relied on Colab's
# preinstalled stack. This file lists ONLY the packages actually imported by the
# code in src/ and scripts/, grouped by which pipeline stages need them.
#
# Core (data -> detection -> time series -> correlation -> visualisation). These
# are sufficient to run `python scripts/run_pipeline.py`.
numpy>=1.24
pandas>=2.0
matplotlib>=3.7
seaborn>=0.12
PyYAML>=6.0

# Preprocessing: NLTK is used for word/sentence tokenisation when available; the
# code falls back to a regex tokeniser if it is absent, so this is recommended
# but not strictly required for the core stages.
nltk>=3.8

# Topic modelling stage (Sentence-BERT embeddings + K-means + Davies-Bouldin).
scikit-learn>=1.3
sentence-transformers>=2.2

# Sentiment stage (RoBERTa via HuggingFace). torch is the backend for transformers.
transformers>=4.30
torch>=2.0

# Testing.
pytest>=7.0
